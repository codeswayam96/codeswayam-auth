/**
 * Invoices Layout
 * Reuses the same top-nav + sidebar shell as /account/* pages.
 * The /invoices route lives outside /account/ so it needs its own layout.
 */
export { default } from "@/app/account/layout";
